/**
 * Spiritual features (daily Bible verse + prayer requests) are kept in the
 * codebase but hidden from the product UI. Flip to `true` to re-enable.
 */
export const SPIRITUAL_FEATURES_ENABLED = false
